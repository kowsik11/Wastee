{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {"outputs": []},
    "test": {"dependsOn": ["lint"], "outputs": []},
    "build": {"dependsOn": ["test"], "outputs": []},
    "dev": {"cache": false}
  }
}
